[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "codex-meter"
version = "0.1.0"
description = "Offline-first Codex usage reports for tokens, credits, costs, sessions, and projects."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
  { name = "Rajdeep Mondal" }
]
keywords = ["codex", "openai", "usage", "tokens", "cli", "credits"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development",
  "Topic :: Utilities"
]
dependencies = [
  "rich>=13.7",
  "typer>=0.12"
]

[project.optional-dependencies]
prom = ["prometheus-client>=0.20"]

[project.urls]
Homepage = "https://github.com/rajdeepmondal/codex-meter"
Repository = "https://github.com/rajdeepmondal/codex-meter"
Issues = "https://github.com/rajdeepmondal/codex-meter/issues"

[project.scripts]
codex-meter = "codex_meter.cli:app"

[dependency-groups]
dev = [
  "build>=1.2",
  "prometheus-client>=0.20",
  "pytest>=8.2",
  "pytest-cov>=5.0",
  "ruff>=0.6",
  "typer>=0.12"
]

[tool.hatch.build.targets.wheel]
packages = ["src/codex_meter"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["B008"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.coverage.run]
source = ["src/codex_meter"]
branch = true

[tool.coverage.report]
exclude_also = [
  "if __name__ == .__main__.:",
  "raise NotImplementedError",
  "pragma: no cover",
]
show_missing = true
