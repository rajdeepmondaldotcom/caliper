# Local environment overrides for release checks.
#
# Copy to .env (git-ignored) only if you need local overrides. Normal PyPI
# publishing uses GitHub Actions Trusted Publisher OIDC and does not require
# a local PyPI token.
#
# Example:
# CALIPER_SMOKE_ALLOW_NETWORK=0
