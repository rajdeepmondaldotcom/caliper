# Caliper Migration Notes

This directory records compatibility notes for schema-stabilizing releases.

Each note names the CLI version, the JSON `schema_version`, compatibility
impact, and the exact consumer action required.
